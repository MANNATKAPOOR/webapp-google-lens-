name: ai_vision_ocr
description: AI Vision OCR Platform - Google Lens style image analysis web application
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  file_picker: ^8.0.0
  google_fonts: ^6.1.0
  pdf: ^3.10.0
  url_strategy: ^0.3.0
  shimmer: ^3.0.0
  flutter_animate: ^4.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
